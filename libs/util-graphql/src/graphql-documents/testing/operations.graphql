query findUser($userId: ID!) {
  user(id: $userId) {
    ...UserFields
  }
}

query allUsers {
  allUsers {
    address
  }
}

fragment UserFields on User {
  id
  address
  balances {
    currency
    amount
  }
  twentyFourHourChange
}
