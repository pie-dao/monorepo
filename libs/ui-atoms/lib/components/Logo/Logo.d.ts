/// <reference types="react" />
import PropTypes from "prop-types";
import "./logo.css";
declare const Logo: {
    ({ size }: {
        size: any;
    }): JSX.Element;
    protoTypes: {
        size: PropTypes.Requireable<string>;
    };
};
export default Logo;
