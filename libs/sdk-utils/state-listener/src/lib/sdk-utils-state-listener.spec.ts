import { sdkUtilsStateListener } from './sdk-utils-state-listener';

describe('sdkUtilsStateListener', () => {
  it('should work', () => {
    expect(sdkUtilsStateListener()).toEqual('sdk-utils-state-listener');
  });
});
